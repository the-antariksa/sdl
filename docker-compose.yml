services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sdl-cargo-web
    ports:
      - "3000:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
